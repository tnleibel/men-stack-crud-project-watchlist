<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= movie.name %></title>
    <link rel="stylesheet" type="text/css" href="/css/style.css"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@200..700&display=swap" rel="stylesheet">
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1 class="header">Edit <%= movie.name %></h1>
    <form class="input-flex-container" action="/users/<%= user._id %>/watchlist/<%= movie._id %>?_method=PUT" method="POST">
        <div class="edit-page-form">
                <div class="flex-input">
                    <label class="input-label" for="name">Name:</label>
                    <input class="input" type="text" name="name" id="name" value="<%= movie.name %>" required/>
                </div>
                <div class="flex-input">
                    <% if(movie.isWatched === true){ %>
                        <label class="input-label" for="isWatched">Watched?</label>
                        <input class="input" type="checkbox" name="isWatched" id="isWatched" checked="checked"/>
                    <% } else { %>
                        <label class="input-label" for="isWatched">Watched?</label>
                        <input class="input" type="checkbox" name="isWatched" id="isWatched"/>
                    <% } %>
                </div>
                <div class="flex-input">
                    <label class="input-label" for="genre">Genre:</label>
                    <input class="input" type="text" name="genre" id="genre" value="<%= movie.genre %>"/>
                </div>
                <div class="flex-input">
                    <label class="input-label" for="director">Director:</label>
                    <input class="input" type="text" name="director" id="director" value="<%= movie.director %>"/>
                </div>
                <div class="flex-input">
                    <label class="input-label" for="yearReleased">Release Year:</label>
                    <input class="input" type="text" name="yearReleased" id="yearReleased" value="<%= movie.yearReleased %>"/>
                </div>
                <div class="flex-input">
                    <label class="input-label" for="notes">Review/Synopsis:</label>
                    <input class="input" type="textarea" name="notes" id="notes" value="<%= movie.notes %>"/>
                </div>
            </div>
        <button class="button" type="submit">Edit Movie</button>
    </form>
</body>
</html>